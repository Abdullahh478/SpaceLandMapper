# SpaceLandMapper

AI-based land classification project using EuroSAT satellite imagery.

## Project structure
- `data/` dataset files and split files
- `src/` training and preprocessing scripts
- `outputs/` saved metrics, predictions, and figures
- `dashboard/` Streamlit app files
- `docs/` project documents
- `experiments/` logs and notes
- `notebooks/` testing and exploration notebooks